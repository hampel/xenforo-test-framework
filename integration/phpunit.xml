<?xml version="1.0" encoding="UTF-8"?>
<!--
  This package's own integration tests. They need a XenForo install and are skipped
  without one, so they can never run in CI - see CLAUDE.md.

  failOnRisky is absent for the same reason as in the shipped scaffold: XF::start()
  leaves error and exception handlers installed, so every test here is "risky".
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         backupGlobals="false"
         backupStaticProperties="false"
         bootstrap="../vendor/autoload.php"
         cacheDirectory="../.phpunit.cache"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true">
  <testsuites>
    <testsuite name="Integration">
      <directory suffix="Test.php">.</directory>
    </testsuite>
  </testsuites>
</phpunit>
